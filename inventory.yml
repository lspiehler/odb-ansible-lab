all:
  hosts:
    ODBTST:
      host_var: host_value
      ansible_user: azureuser
      ansible_host: 10.40.44.4
  vars:
    global_var: global_value
  children:
    linux:
      hosts:
        ODBTST:
      vars:
        group_var: group_value
